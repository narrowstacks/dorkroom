# Supabase Configuration
# Copy this file to .env and fill in your actual values

# Your Supabase project URL
SUPABASE_ENDPOINT=https://your-project-id.supabase.co

# Your Supabase service role key (for server-side operations)
# WARNING: This key has admin privileges - keep it secure!
SUPABASE_MASTER_API_KEY=your_supabase_service_role_key_here

# Unkey configuration (API key verification + rate limiting)
UNKEY_ROOT_KEY=unkey_root_key_here
UNKEY_API_ID=api_your_unkey_api_id
# Required permission checked during key verification (scopes valid keys)
UNKEY_API_KEY_PERMISSION=dorkroom.api.access
# Optional: pre-created Unkey ratelimit namespace for anonymous traffic
# If omitted, defaults to "<UNKEY_API_ID>-anonymous".
UNKEY_ANON_NAMESPACE=api_your_unkey_api_id-anonymous
# Admin key for programmatic key management scripts (create/update tiers)
# Keep this server-side only; do not expose to client code.
UNKEY_ADMIN_ROOT_KEY=unkey_admin_root_key_here

# Optional: Development environment flag
NODE_ENV=development
