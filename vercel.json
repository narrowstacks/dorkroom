{
  "buildCommand": "bunx nx build dorkroom",
  "outputDirectory": "apps/dorkroom/dist",
  "devCommand": "bunx nx serve dorkroom",
  "installCommand": "bun install -y hostname",
  "framework": null,
  "routes": [{ "handle": "filesystem" }, { "src": "/(.*)", "dest": "/" }]
}
