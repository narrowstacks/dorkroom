{
  "buildCommand": "npx nx build dorkroom",
  "outputDirectory": "apps/dorkroom/dist",
  "devCommand": "npx nx serve dorkroom",
  "installCommand": "npm install",
  "framework": null,
  "routes": [{ "handle": "filesystem" }, { "src": "/(.*)", "dest": "/" }]
}
