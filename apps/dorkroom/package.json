{
  "name": "@dorkroom/dorkroom",
  "version": "2026.08.10",
  "private": true,
  "license": "AGPL-3.0-only",
  "scripts": {
    "dev": "vite --host=0.0.0.0",
    "build": "vite build",
    "lint": "oxlint && biome check --linter-enabled=false .",
    "test": "vitest run",
    "typecheck": "node ../../node_modules/typescript-7/bin/tsc --noEmit --project tsconfig.app.json"
  }
}
