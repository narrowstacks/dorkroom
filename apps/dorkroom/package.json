{
  "name": "@dorkroom/dorkroom",
  "version": "0.0.1",
  "private": true,
  "license": "AGPL-3.0-only",
  "dependencies": {
    "@dorkroom/api": "workspace:*",
    "@dorkroom/logic": "workspace:*",
    "@dorkroom/ui": "workspace:*"
  },
  "scripts": {
    "dev": "vite --host=0.0.0.0",
    "build": "vite build",
    "lint": "bunx oxlint --type-aware --tsconfig tsconfig.app.json --react-plugin --jsx-a11y-plugin .",
    "test": "vitest run",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "typecheck": "bunx tsgo --noEmit"
  }
}
